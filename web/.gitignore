# Vom Build erzeugte Artefakte (rust/build_wasm.py) -- nicht einchecken.
dhrt.js
dhrt.wasm
dhrt.data
program.dh
program.dhc
# Kopie des assets/-Ordners neben der .dh -- Quelle ist das Programm selbst.
assets/
